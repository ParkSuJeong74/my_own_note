# T1 match page

## Purpose

The `/t1` page collects T1 schedules, series results, set drafts, game statistics, and the official
Player of the Match in one match list.

## Finished-match POM

When a match is `FINISHED` and an official POM player has been synchronized, the player name is
shown directly on the collapsed match card so it can be found without opening set details. Upcoming
and live matches never show a POM, and finished matches without official POM data omit the badge
rather than displaying a placeholder. The expanded winning-match detail continues to show the full
POM panel with series statistics when those details are available.

This is a presentation-only change. It does not alter provider synchronization, stored match data,
scores, or notification behavior. Verify finished, unfinished, blank-POM, and whitespace inputs,
responsive card layout, the full test suite, type-check, and production build.
