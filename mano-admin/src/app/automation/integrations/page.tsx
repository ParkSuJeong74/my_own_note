import {dispatchN8nEventsAction} from "@/app/automation/actions";
import {AutomationHelp} from "@/components/automation-help";
import {getN8nIntegrationStatus,listRecentAutomationEvents} from "@/lib/n8n-integration";
export const dynamic="force-dynamic";
export default async function IntegrationsPage(){const [status,events]=await Promise.all([getN8nIntegrationStatus(),listRecentAutomationEvents(30)]);return <><header className="page-head"><div><p className="eyebrow">AUTOMATION</p><h1>외부 연결</h1><p>n8n 이벤트 전달 상태를 확인합니다.</p></div></header><AutomationHelp steps={["평소에는 확인할 필요가 없습니다.","오류나 Pending이 계속 쌓일 때만 재전송하세요."]} risk="재전송하면 n8n 작업이 다시 실행될 수 있습니다. 이미 처리된 작업인지 먼저 확인하세요."/><section className="integration-summary"><article><span>n8n</span><strong>{status.configured?"연결됨":"연결 안 됨"}</strong></article><article><span>전송 대기</span><strong>{status.pending}</strong></article><article><span>전송 완료</span><strong>{status.delivered}</strong></article><article><span>오류</span><strong>{status.failedDelivery}</strong></article></section><div className="integration-actions"><form action={dispatchN8nEventsAction}><button disabled={!status.webhookConfigured||status.pending===0}>대기 이벤트 재전송</button></form><small>{status.lastDeliveredAt?`마지막 전달 ${new Date(status.lastDeliveredAt).toLocaleString("ko-KR")}`:"아직 전달된 이벤트가 없습니다."}</small></div><section className="data-list integration-events">{events.map(e=><article key={e.id}><div className="data-main"><div className="data-meta"><span>#{e.id}</span><span>{e.deliveryStatus}</span></div><h2>{e.eventType}</h2><p>{e.occurredAt}</p></div><code>{e.eventKey}</code></article>)}</section></>}
