import { ntfyConfigured } from "@/lib/ntfy";

export const dynamic = "force-dynamic";

export default async function NotificationsPage({ searchParams }: { searchParams: Promise<{ test?: string }> }) {
  const result = (await searchParams).test, configured = ntfyConfigured();
  return <><header className="page-head"><div><p className="eyebrow">PERSONAL</p><h1>Notifications</h1><p>ntfy 연결 상태를 확인하고 실제 기기로 테스트 알림을 보냅니다.</p></div></header><section className="notification-grid"><article><span>ntfy 상태</span><strong className={configured?"configured":"not-configured"}>{configured?"연결 설정됨":"설정 필요"}</strong><small>토큰과 topic 값은 화면에 표시하지 않습니다.</small></article><article><span>T1 알림</span><strong>10분 전 · 세트 결과 · 경기 종료</strong><small>경기별 상태를 비교하며 각 알림은 한 번만 전송됩니다.</small></article></section><section className="notification-test"><div><p className="eyebrow">TEST</p><h2>테스트 알림 보내기</h2><p>버튼을 누르면 현재 설정된 topic으로 테스트 메시지를 전송합니다.</p></div><form action="/api/notifications/test" method="post"><button disabled={!configured}>ntfy 테스트 보내기</button></form></section>{result==="sent"&&<p className="notification-result success">테스트 알림을 전송했습니다. ntfy 앱에서 확인해 주세요.</p>}{result==="failed"&&<p className="notification-result failure">전송에 실패했습니다. Error Logs와 ntfy 환경변수를 확인해 주세요.</p>}{result==="not-configured"&&<p className="notification-result failure">NTFY_BASE_URL과 NTFY_TOPIC을 먼저 설정해 주세요.</p>}<section className="notification-guide"><h2>T1 자동 알림</h2><ol><li><code>POST /api/t1/sync</code>로 일정 전체를 하루 한 번 동기화합니다.</li><li>n8n에서 10분마다 <code>POST /api/t1/monitor</code>를 호출합니다.</li><li><code>startLiveMonitoring</code>이 true일 때만 <code>/api/t1/live-monitor</code>를 1분 간격으로 반복합니다.</li><li><code>finished</code>가 true이면 Live loop를 종료합니다.</li></ol></section></>;
}
