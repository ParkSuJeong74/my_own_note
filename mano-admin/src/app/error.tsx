"use client";
import {useEffect} from "react";
const isStaleAction=(message:string)=>/Server Action .* was not found on the server|Failed to find Server Action/i.test(message);
export default function ErrorBoundary({error,reset}:{error:Error&{digest?:string};reset:()=>void}){const stale=isStaleAction(error.message);useEffect(()=>{if(stale)return;void fetch("/api/errors",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({source:"admin-ui",message:error.message,digest:error.digest})});},[error,stale]);if(stale)return <div className="empty-state"><h2>관리 화면이 새 버전으로 배포되었습니다.</h2><p>열어둔 화면은 이전 버전이라 저장할 수 없습니다. 새 버전을 불러온 다음 다시 등록해 주세요.</p><button onClick={()=>window.location.reload()}>새 버전 불러오기</button></div>;return <div className="empty-state"><h2>이 페이지를 불러오지 못했습니다.</h2><p>오류가 Error Logs에 기록되었습니다.</p><button onClick={reset}>다시 시도</button></div>}
