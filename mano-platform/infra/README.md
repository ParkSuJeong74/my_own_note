# Infrastructure

Service-owned local development and deployment infrastructure will live here. It must not reference
the parent repository at runtime. Initial infrastructure work begins after persistence, object
storage, backup, and deployment ADRs are accepted.

