# @mano/contracts

Versioned request, response, event, error, identifier, and compatibility contracts. The package uses
dependency-free strict validators for the initial `/v1` mutation boundary; an API framework or schema
library may replace the implementation only when compatibility tests remain unchanged. See
`docs/11-api-contract-v1.md`.
