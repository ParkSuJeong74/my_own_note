# @mano/api

Future service API for identity, pages, blocks, databases, files, search, synchronization,
notifications, integrations, publishing, and administration. Runtime and persistence choices are
pending ADRs.

