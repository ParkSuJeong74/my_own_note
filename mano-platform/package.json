{
  "name": "mano-platform",
  "version": "0.0.0",
  "private": true,
  "description": "Self-contained monorepo for the Mano personal workspace platform",
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "validate": "node scripts/validate-workspace.mjs",
    "check": "pnpm validate && pnpm build:core && pnpm typecheck && pnpm test && pnpm build",
    "check:release": "pnpm check && pnpm --filter @mano/web test:e2e",
    "check:cloudflare": "pnpm check:release && pnpm --filter @mano/web check:vinext && pnpm --filter @mano/web build:vinext",
    "build:core": "pnpm --filter @mano/editor-core build",
    "test": "pnpm -r --if-present test",
    "typecheck": "pnpm -r --if-present typecheck",
    "build": "pnpm -r --if-present build"
  },
  "devDependencies": {
    "@types/node": "22.20.3",
    "typescript": "7.0.2",
    "vitest": "5.0.1"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "workerd"
    ]
  }
}
